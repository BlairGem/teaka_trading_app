# Task 3: full recovered paper runtime

Base: `5e74483094d951961cf75c963c3dd8f4387c08c6`. Implementation commit is the commit containing this report.

## Result and verification

The recovered Flask application now has an explicit package factory. Its recovered ORM users, strategies, signals, executions and backtest results are used by the real `trading_stack.trading_engine.run_trading_engine` paper loop. PaperRun and PaperDecision rows preserve run summaries and decisions. Task1's broker supplies shared cash/accounting; Task2's signal generator, strategy normalization, equity normalization, position sizing, shared risk checks, local OHLCV normalization and backtest functions are used directly.

All shell commands below ran from `D:\EV_AI\Worktrees\teaka-paper-integration-20260907` with PowerShell profile loading disabled. No new dependency installation, external manager initialization, credential/account use, private database read, source move/deletion, listener, service, push or PR was performed.

- Safety boundary added before baseline project imports: the existing audit guard now rejects every `sqlite3.connect` target except the exact `:memory:` target, in addition to its socket/process/file restrictions.
- Initial RED: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration` -> 9 tests, 8 errors from the recovered app's non-package `database` import; persistent SQLite guard test passed. Artifact directory: `paper_trading/state/test-artifacts/run-2c4f4e698d4c46249277431a4a872b3e`.
- Initial integrated GREEN, same command -> 9 tests, OK, exit 0. Artifacts: `run-ce9bf4a4f2554b78aa108bef02b5df5b`.
- Focused RED: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration.FullPaperTests.test_exit_rules_use_prior_candle_next_open_and_simulated_fill_times tests.test_full_paper_integration.FullPaperTests.test_invalid_manual_values_and_user_limits_fail_closed` -> 2 failures: exit occurred at 04:00 instead of 02:00 and nonfinite user position limit did not reject. Artifacts: `run-878d838a9a4b48bb961f96025fc0700a`.
- Further focused RED: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration.FullPaperTests.test_owned_backtest_api_uses_local_candles_and_foreign_owner_fails tests.test_full_paper_integration.FullPaperTests.test_csv_after_balance_read_and_repeat_preserves_economics tests.test_full_paper_integration.FullPaperTests.test_finite_cli_uses_real_orm_and_writes_complete_report` -> 1 failure, 1 error, CLI check passed: backtest route unavailable and balance-only read blocked CSV. Artifacts: `run-63522b3a40a944109dba7b67accd42fe`.
- Clock precondition RED: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration.FullPaperTests.test_submit_without_replay_clock_fails_before_mutation` -> 1 error: missing clock caused failure after broker mutation. Corrected to reject before account creation; regression passes.
- Legacy helper RED: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration.FullPaperTests.test_engine_position_and_order_status_helpers_use_paper_state` -> 1 error: helper attempted absolute `broker_apis` import. Public helpers now use the injected paper runtime; regression passes.
- Final complete GREEN: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py` -> `Ran 90 tests in 7.826s`, `OK`, exit 0. This includes all earlier 67 tests and 23 full integration tests. Artifacts: `paper_trading/state/test-artifacts/run-fad649be58d9411ebaa8a6656f683965`.
- `git diff --check` -> exit 0 after preserving LF line endings and removing changed-line whitespace. The final whitespace-only cleanup did not change runtime behavior.

Integration evidence includes two simultaneously held same-quote strategies with distinct owned lots/shared cash, two competing strategies sharing a pair with rejection, actual nested-equity sizing (5 units for 0.1% risk at 100/98 on 10000 equity), requested manual sizing, fees/slippage, final liquidation, owned close under new-order/kill limits, same-candle stop protection, exit rules at prior-candle/next-open timing, exact direct-candle deduplication, economic and event-time determinism, user/API ownership, synthetic recovered login, invalid numeric/market inputs, inherited-live flags, model inactivity, web roots, preserved existing outputs, and finite CLI execution.

## Public interfaces

- `trading_stack.app.create_app(settings=None, runtime=None) -> Flask`: importing the module does not create an app/database. Factory initializes an isolated SQLite memory database, recovered auth and page routes, and paper API routes. Settings may use `sqlite://` or `sqlite:///:memory:` only; inherited DATABASE_URL is ignored. App mode and both live/private flags are forcibly paper/false before runtime/blueprint initialization. No module-level `app` instance remains.
- `LocalCandleProvider(frames: dict[pair, DataFrame], data_origin: str, timeframe='1h')`: copies/validates OHLCV, rejects duplicate timestamps and non-USDT quote pairs. Naive timestamps mean UTC; aware timestamps normalize to naive UTC. Initial clock is unset and exposes no data. `advance(when)` is monotonic. `get_latest_prices(pairs, as_of=None)` caps at both its clock and supplied bound. `get_historical_data(pair, timeframe, *, start_date=None, end_date=None, limit=100)` likewise caps at the clock.
- `PaperRuntime(provider, config: PaperConfig | None=None)`: one broker/account per ORM user, one shared dataset/clock per runtime. `account(user_id)`, `balance(user_id)`, `positions(user_id)`, `manual_order(user_id, pair, side, quantity)`, `close(user_id, position_id, price=None, reason='manual_close')`, `replay(user_id, start, end)`. Positions carry stable ORM execution IDs and strategy ownership; a strategy cannot close another strategy's lot. Manual SELL addresses only manually owned exposure; explicit owned-position close can close a strategy lot.
- `run_trading_engine(runtime=None, user_id=None, candle_time=None)`: finite one-candle call. Omitted runtime resolves the current app's `paper_runtime` extension. User and candle time are required. Stable strategy-ID order, per-user/strategy/pair/candle deduplication plus processed-candle guard. Signals use only prior completed history, fill at current open, then stops/takes apply, then close marking. No short opening. A gap through a stop fills at the worse open; a candle touching both thresholds uses stop first.
- `execute_trade_from_signal(signal, position_size, runtime=None, user_id=None)`: requires explicit owner and pending signal, uses the actual requested quantity, and never dispatches live. Public `get_active_positions`, `get_position_details`, `close_position`, and `update_orders_status` use paper state; orders are synchronous.
- `PaperBroker(config, log_path=None, clock=None)`: backward-compatible optional clock collaborator. Runtime injects simulated ISO time directly; fill timestamps are never rewritten after recording. Economic/event-time deterministic comparisons exclude only generated order IDs.
- `paper_replay.replay_csv(runtime, user_id, csv_path, pair, timeframe, start, end, data_origin, output_path)`: finite load/run/export, CSV requires exactly timestamp/open/high/low/close/volume, at most 20 MB/100000 rows, nonempty origin, new `.json` target. Source path and SHA256 are exported. Existing files are refused. Failed reserved outputs are preserved rather than deleted.

## API routes and UI handoff

Authenticated paper routes retain `/api/account-balance`, `/api/prices`, `/api/chart-data`, `/api/positions`, `/api/positions/<id>/close`, `/api/trades`, `/api/execute-trade`, `/api/signals`, `/api/signals/<id>/execute`, `/api/strategies` and `/api/strategies/<id>` CRUD, strategy risk metrics, portfolio risk/exposure, owned `/api/backtests` POST and `/api/backtests/<id>` GET. Strategy DELETE deactivates to preserve owned historical records. Paper manual order success maps the actual broker fill to `data.status='filled'`, `execution_id`, `price`, `amount`, fees and virtual order ID. All owner-sensitive operations filter by current user.

`GET /api/paper/runs` returns the current user's ORM run records. `POST /api/paper/replay` accepts `csv_path`, `trading_pair`, `timeframe`, `start`, `end`, `data_origin`. Configure explicit existing `PAPER_DATA_ROOT` and `PAPER_OUTPUT_ROOT` in factory settings. CSV paths must resolve inside the data root (including symlink targets); output names are generated as unique `paper-run-<uuid>.json` inside the output root. Client output_path is not used. Without configured roots, replay is HTTP 400. A foreign strategy/signal/backtest is HTTP 404. Optional ML, MATLAB, futures and websocket endpoints explicitly return HTTP 503/unavailable. The legacy additional indicator/support-resistance/pattern endpoints are also currently explicit unavailable; deterministic SMA/EMA/RSI/MACD remain available to the actual engine/backtest computations.

No Task4 templates/UI edits were made. Recovered page functions were nested under registration so the factory can own app construction; dashboard's existing placeholder context remains for Task4 to replace with runtime state. Existing HTML may assume older response shapes/config values and needs that planned integration. The root `app.py` remains untouched; use the package factory above, not that placeholder app.

## Finite replay and startup

The CLI entry is `trading_stack.paper_replay.main(argv=None)` / `python -m trading_stack.paper_replay` from the isolated worktree. Required flags: `--csv`, `--strategies` (local JSON list), `--pair`, `--timeframe`, `--start`, `--end`, `--data-origin`, `--output`. Each strategy definition has name, indicators_config, entry_conditions, optional exit_conditions, risk_per_trade_pct, stop_loss_pct and take_profit_pct. The CLI creates only a memory ORM fixture user (`local_replay`, no login credential) and the requested strategy definitions. It never loads recovered users or a DB. For isolated `-I` invocation, insert the explicitly verified worktree path in sys.path and call this entry; the guarded CLI test demonstrates this route without subprocess/network permission.

Complete observed finite CLI output is preserved at `paper_trading/state/test-artifacts/run-fad649be58d9411ebaa8a6656f683965/cli-complete-output.txt`; matching JSON is `cli-replay.json`, with input `cli-candles.csv` and `cli-strategies.json` beside it. It states `synthetic_test_fixture`, 2026-01-01T00:00:00 through 04:00:00 UTC, 5 candles, 9 decision rows, 2 fills, 10 bps fees and 5 bps slippage. Virtual final cash/equity is 10018.470010000001 USDT, realized P&L 18.470010000000073 and fees 1.01999. Entry: 5 units, requested 100, fill 100.05 at 01:00; exit: 5 units, requested 104, fill 103.94800000000001 at 04:00. This is a labeled synthetic accounting example, not recovered performance.

For Task4's authorized contained listener, import `create_app` after inserting the verified worktree path and call `app.run(host='127.0.0.1', port=<chosen-free-port>, debug=False, use_reloader=False, threaded=False)`. Pass explicit local data/output roots and an injected fixture runtime or register synthetic users via the recovered login flow. This task did not start a listener; offline acceptance precedes that UI work.

## Explicit limits and self-review

- Memory ORM/account state intentionally lasts only for the app process. No persistence, recovery/resumption, schedulers, providers, real account connection, or multiworker architecture was added. A single loopback process is the startup contract.
- Accounting is USDT only; default broker allowed symbols are BTC-USDT/ETH-USDT/SOL-USDT. A configured allowed-symbol list may extend this to supported USDT pairs. No FX conversion or mixed-currency aggregation is implied.
- One runtime owns one dataset/clock. Reading balances can initialize accounts without preventing first CSV load. After economic activity, changing CSV identity/hash/pair/timeframe/origin is refused. Same caller/same CSV/range returns its cached run without duplicating fills; each requested export still requires a new output filename. A later other-user replay of an earlier range is rejected before changing accounts/clock. An independent historical replay or different dataset requires a fresh runtime/app. No user's request can rewind or replace data under another user's economic state through the web/CSV entry.
- Engine economics are deterministic floating-point simulations. Entry/exit rules use completed bars and next opens, protection applies intrabar, and final liquidation uses final close. Fees/slippage are explicit. Wall-clock ORM creation metadata and generated record IDs are not strategy inputs.
- ML artifacts, TA-Lib-only/custom MATLAB indicators, MATLAB, futures, messaging and provider managers remain inactive. Existing inactive legacy source is retained behind separate functions; the new factory registers only paper handlers and public engine functions choose paper state.
- Reviewed checks: factory before DB initialization; no inherited persistent URI; no manager import in guarded inherited-live runs; actual manual quantity/risk context; normalized nested equity in the real loop; user/strategy lot ownership; replay clock/filtering; held close bypass; complete finite export; all 90 tests and final diff whitespace check.

## Review fix round 1

All four Important findings in `task-3-review.md` were reproduced at base `6fe9f3a35278c0e37df67736a3dad156ce6e7ebf` and addressed together. No app/UI/listener, durable database, provider, installation or external-operation scope was added.

Exact focused RED and GREEN command (PowerShell, worktree cwd, profile disabled):

```powershell
& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration.FullPaperTests.test_other_user_forward_replay_cannot_skip_held_account_events tests.test_full_paper_integration.FullPaperTests.test_same_user_direct_engine_and_replay_gaps_reject_before_mutation tests.test_full_paper_integration.FullPaperTests.test_direct_provider_advancement_cannot_bypass_held_account_clock tests.test_full_paper_integration.FullPaperTests.test_inactive_replay_prevents_dataset_replacement_and_false_provenance tests.test_full_paper_integration.FullPaperTests.test_manual_signal_uses_requested_quantity_but_preserves_strategy_max_risk tests.test_full_paper_integration.FullPaperTests.test_strategy_metrics_include_owned_entry_and_each_signal_less_close
```

- RED: `Ran 6 tests in 3.462s`, `FAILED (failures=9)`, exit 1. All clock/no-replacement/requested-size expectations failed; metrics counted only entries for each of stop, take, final liquidation and explicit close. Artifacts: `paper_trading/state/test-artifacts/run-85cb1d01766f4460a4922a999a8bc632`.
- GREEN, same six-test command: `Ran 6 tests in 2.035s`, `OK`, exit 0. Artifacts: `run-151be0124dce4eaab9eb6fd429b8b9d7`.
- Broader focused GREEN: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py tests.test_full_paper_integration` -> `Ran 29 tests in 7.719s`, `OK`, exit 0. Artifacts: `run-bbf24990074542f4934c9b45cf544b1b`.
- One final complete suite: `& ./.venv-paper/Scripts/python.exe -I tests/run_offline.py` -> `Ran 96 tests in 9.767s`, `OK`, exit 0. Artifacts: `run-2999ba66d83e4b6e8ff8cfea50a94662`. Existing memory-only SQLite, socket, process and artifact-write guards remained active.
- `git diff --check` -> exit 0.

Interface/invariant changes:

1. `PaperRuntime.validate_clock_advance(user_id, when, scheduled_candles=())` is a non-mutating preflight. Replays validate their complete planned candle sequence before creating a PaperRun/account; direct engine calls validate before marking, generating signals or executing. While any other user holds exposure, advancement fails with a `held account` reason. A held account's owner may advance only through all known intervening candles; skipped candles fail with `unprocessed candle`. The direct provider `advance` method is also bound to the runtime guard, so calling it outside the engine cannot bypass held exposure. Engine calls hold the runtime lock. The regression verifies unchanged clock/cash/lots/run/execution/account state after rejection, then confirms the owner can process the sequence and receive the actual t2 stop at 98. This deliberately conservative shared-clock policy can require other held accounts to close at the current clock before one account advances; no independent replay sessions were introduced.
2. Different CSV identity is now rejected after any processed candle, cached run, strategy decision or broker fill, including no-active-strategy/no-fill replays. Direct provider replacement has the same history boundary. Balance-only initialization still permits the first dataset, and identical CSV/range replay remains idempotent. Rejection preserves the original provider, clock, cache and candle guards; new source hashes cannot be attached to an old cached result.
3. `check_risk_limits(..., requested_quantity=None, stop_loss=None)` adds optional backward-compatible inputs. With a requested quantity, it checks that quantity against the actual selected strategy's risk budget using the actual protective stop, then uses that same quantity for exposure. Default callers retain calculated sizing. The runtime always passes actual quantity/stop and uses the explicit 1% manual budget for orders without a strategy. The reviewed 2%-strategy-risk/80%-position-cap example now accepts quantity 1, while a requested quantity exceeding the actual strategy risk maximum remains rejected.
4. `TradeExecution.strategy_id` is an indexed nullable FK independent of signal_id. Every paper strategy execution, including stop/take/final/explicit closes, persists it. Strategy risk metrics filter by both current-user ownership and this column rather than inner-joining signals. Regression verifies all four closing causes have strategy_id with null signal_id, total execution counts and fees include both sides, a foreign-user execution cannot inflate totals, and a foreign user's metrics request is 404. The database remains memory-only; no migration/read of a persistent database occurred.

Readiness claim is limited to the reviewed paper backend and enforced clock/economic ownership invariants. Task4 still owns dashboard/template/API-shape and credential/notification UI cleanup plus the authorized contained browser/listener validation.
